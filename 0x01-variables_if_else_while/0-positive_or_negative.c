#include <stdio.h>
/**
   * main - Entry point
   *
   * Description: 'checke if a number is negative postibe or zero'
   * Return: Always 0 (Success)
 */
int main(void)
{
if (n > 0)
{
	printf("%d is positive\n", n);
}
else if (n == 0)
{
printf("%d is zero", n);
}

else if (n < 0)
{
printf("%d is negative", n);
}

return (0);
}

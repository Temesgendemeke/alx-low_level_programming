#include "main.h"
#include <stdio.h>
/**
 * _abs - computes the absolute value of an integer
 * @n: - is number to be checked
 * Return: return absoulte value.
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else if (n < 0)
{
return(-n);
}

}
